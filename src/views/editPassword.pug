extends base

block content
    form(method="post")
        if errorMessage 
            span=errorMessage
        input(type="password", placeholder="Enter Your password", name="oldPassword", required)
        input(type="password", placeholder="Enter Your new password", name="password1", required)
        input(type="password", placeholder="Enter Your new password", name="password2", required)
        input(type="submit", value="Save")